language: ruby
rvm:
  - 2.2.3
bundler_args: --jobs=2
env:
  SECRET_KEY_BASE="$(bundle exec rake secret)"
#   DB=postgresql
script:
  - bundle exec rake db:setup
  - bundle exec rake test
cache: bundler
services:
  - postgresql
addons:
  postgresql: "9.4"
