= content_for(:title) do
  Locations

= content_for(:sidebar) do
  = render 'sidebar'

%table.table.entries
  %thead
    %tr
      %th Name
  %tbody
    - @locations.each do |location|
      %tr
        %td= location.name
        %td= link_to "Show", location
