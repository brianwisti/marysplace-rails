= content_for(:title) do
  Cannot Shop

= content_for(:sidebar) do
  = render 'sidebar'

%p 
  These Client Members are unable to shop today. They may have flags
  or have already shopped this week.

%table.table
  %thead
    %tr
      %th Client Member
  %tbody
    - @clients.each do |client|
      %tr
        %td= render '/clients/info_link', client: client
