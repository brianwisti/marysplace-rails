<div class="navbar navbar-default navbar-static-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <%= link_to image_tag('marys-place-icon.png') + " Mary's Place", 
          root_url,
          class: "navbar-brand" %>
      <span class="pull-right">
        <%= link_to current_user.login, current_user, class: "btn navbar-btn" %>
        <% if message_count > 0 %>
          <%= link_to "#{message_count} News #{'Item'.pluralize(message_count)}",
              messages_path,
              class: "badge badge-important" %>
        <% end %>
      </span>
    </div> <%# end navbar-header %>
  </div>   <%# end container %>
</div>     <%# end navbar %>
