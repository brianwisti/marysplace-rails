= content_for(:title) do
  Resolved Client Flags

= content_for(:context_navigation) do
  = render 'context_navigation'

= paginate @client_flags

%table.table
  = render @client_flags

= paginate @client_flags
