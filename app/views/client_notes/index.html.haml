= content_for(:title) do
  All Client Notes

= content_for(:context_navigation) do
  = render 'context_navigation'

= paginate @client_notes

= render 'as_table', client_notes: @client_notes

= paginate @client_notes
