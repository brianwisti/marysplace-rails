%div.panel.panel-default
  .panel-heading
    - if message.title
      %h1.panel-title= message.title
    = link_to message.created_at, message
    by
    %em
      = link_to message.author_login, message.author
  .panel-body
    != message.rendered_content
