= content_for(:title) do
  Points Log

= content_for(:context_navigation) do
  = render 'context_navigation'

= paginate @points_entries

= render 'as_table', points_entries: @points_entries

= paginate @points_entries
