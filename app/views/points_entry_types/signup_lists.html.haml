= content_for(:title) do
  Signup Lists

= content_for(:context_navigation) do
  = render 'context_navigation', points_entry_type: @points_entry_type

%table
  %thead
    %tr
      %th Date
  %tbody
    - @signup_lists.each do |list|
      %tr
        %td= @list.signup_at
