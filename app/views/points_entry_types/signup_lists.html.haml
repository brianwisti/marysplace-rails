= content_for(:title) do
  Signup Lists

= content_for(:sidebar) do
  = render 'sidebar', points_entry_type: @points_entry_type

%table
  %thead
    %tr
      %th Date
  %tbody
    - @signup_lists.each do |list|
      %tr
        %td= @list.signup_at
